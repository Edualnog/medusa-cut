# Worker do Medusa Cut (SaaS). Copie pra .env (ignorado pelo git) e preencha.
# Os MESMOS valores do web/.env.local (server-side):
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
KEY_ENCRYPTION_SECRET=

# opcional
# WORKER_POLL_SEC=5

# Legado (CLI local antigo): a chave de IA agora vem por usuario, decifrada por job.
# LLM_API_KEY=
